http:
  routers:
    coolify:
      entryPoints:
        - "https"
      rule: "Host(`coolify.domain.com`)"
      tls: {}
      service: coolify
  services:
    coolify:
      loadBalancer:
        servers:
          - url: "http://10.0.0.45"
        passHostHeader: true
