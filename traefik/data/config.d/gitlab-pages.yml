http:
  routers:
    gitlab-pages:
      entryPoints:
        - "https"
      rule: "Host(`pages.domain.com`)"
      tls: {}
      service: gitlab-pages
  services:
    gitlab-pages:
      loadBalancer:
        servers:
          - url: "http://10.0.0.45"
        passHostHeader: true
