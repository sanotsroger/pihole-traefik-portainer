http:
  routers:
    registry:
      entryPoints:
        - "https"
      rule: "Host(`registry.domain.com`)"
      tls: {}
      service: registry
  services:
    registry:
      loadBalancer:
        servers:
          - url: "http://10.0.0.45"
        passHostHeader: true
